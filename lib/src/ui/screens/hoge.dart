import 'package:flutter/material.dart';

class HogePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hoge Page'),
      ),
      body: Text('This is Hoge Page.'),
    );
  }
}
